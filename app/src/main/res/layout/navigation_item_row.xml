<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/default_padding"
    android:background="@drawable/nav_list_selector"
    >

    <ImageView
        android:id="@+id/icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/nav_item_today_icon_selector"
        android:layout_marginRight="@dimen/default_padding"
        android:layout_marginEnd="@dimen/default_padding"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/navigation_icon_description"
        />

    <sk.simondorociak.weather.android.view.TypefaceTextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@drawable/nav_item_text_color_selector"
        android:textSize="@dimen/nav_item_text_size"
        android:layout_gravity="center_vertical"
        app:font="@string/font_regular"
        />

</LinearLayout>